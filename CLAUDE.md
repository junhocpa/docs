# 미수거래처 안내 메시지 생성 지침

이 문서는 **미수(미납) 내역 엑셀 파일**을 받아 거래처별 **미수 안내 메시지**를
생성할 때 따라야 하는 규칙이다. 사용자가 미수 내역 엑셀(`.xlsx`)을 첨부하고
"미수 안내 메시지 만들어줘" / "미수거래처에 보낼 메시지" 등을 요청하면 이 지침을
적용한다.

---

## 1. 입력 데이터 (엑셀 컬럼)

엑셀 1행은 헤더이고, 각 행은 **거래처 × 청구월 단위의 미수 1건**이다.
한 거래처가 여러 달에 걸쳐 여러 행으로 나타날 수 있다. 사용하는 주요 컬럼:

| 컬럼 | 헤더 | 용도 |
|------|------|------|
| D | 회원명 | 거래처명. **이 값으로 그룹핑**하고 제목/본문에 사용 |
| B | 회원번호 | 회원명이 같아도 회원번호가 다르면 별도 거래처로 취급 |
| E | 담당관리자 | 참고용(메시지에는 미포함) |
| G | 청구월 | 예: `2026/05` → "26년 5월" |
| H | 납부자 휴대전화 | 발송 대상 번호 |
| I | 상품 | 상품명 매핑(아래 표) |
| M | 결제일(납부기간) | 인출 시도일. 예: `2026-06-04` → "6월 4일" |
| O | 청구금액 | 부가세 포함 청구액 |
| R | 미납금액 | **합산 기준 금액**. 안내문의 모든 금액은 이 값 사용 |
| U | 비고 | "세금계산서 발급" 등 부가 표기 근거 |
| V | 결제결과 | 인출 실패 사유 매핑(아래 표) |
| W | 회원구분 | 참고용 |

마지막 행이 `합계`인 경우(A열) 그 행은 집계 검증용으로만 쓰고 메시지 생성 대상에서 제외한다.

---

## 2. 그룹핑 & 합산 규칙

1. **회원번호(B) + 회원명(D)** 기준으로 행을 묶는다.
2. 각 거래처의 **총 미수대금 = 해당 거래처 모든 행의 미납금액(R) 합**.
3. 거래처 내 행은 **청구월(G) 오름차순**(오래된 달 먼저)으로 정렬한다.
4. 같은 달에 상품이 여러 건이면 각각 별도 줄로 표기한다.

> 검증: 모든 거래처의 총 미수대금을 더한 값이 엑셀 `합계` 행의 미납금액과
> 일치하는지 확인하고, 불일치 시 사용자에게 알린다.

---

## 3. 값 매핑

### 상품(I) → 표기명
| 엑셀 상품 | 표기 |
|-----------|------|
| `[2000]월세무기장료(법인)` / `(개인)` | 월 기장료 |
| `[2000]결산조정료(법인)` / `(개인)` | 결산조정료 |
| `[2100]기타수수료` | 기타 수수료 |
| 그 외 `[코드]상품명(구분)` | 대괄호 코드와 괄호 구분을 떼고 상품명만 사용 |

### 결제결과(V) → 실패 사유 표기
| 엑셀 결제결과 | 표기 |
|---------------|------|
| `잔액부족` | 인출 실패 |
| `유효기간경과` | 카드 유효기간 경과로 결제 실패 |
| `거래정지카드` | 거래정지 카드로 결제 실패 |
| `월 사용한도액 초과` | 카드 한도 초과로 결제 실패 |
| 그 외 | 결제결과 텍스트를 그대로 사용 |

### 회원명(D) → 표기명 (법인격 약식 표기)
회원명에 법인격이 풀어쓰기로 들어있으면 약식 괄호 표기로 바꾼다. **원래 위치(앞/뒤)를
유지**하고, 회사명과 붙여 쓰며 사이 공백은 제거한다.

| 원문 | 표기 |
|------|------|
| `주식회사` | `(주)` |
| `유한회사` | `(유)` |
| `유한책임회사` | `(유한)` |
| `합자회사` | `(합자)` |
| `합명회사` | `(합명)` |

예: `주식회사 하늘드림홀딩스` → `(주)하늘드림홀딩스`,
`알이컴퍼니 주식회사` → `알이컴퍼니(주)`.
이미 `(주)` 등 약식으로 들어온 경우 그대로 둔다. 법인격 표기가 없는 상호는 변경하지 않는다.

### 세금계산서 발급 표기
비고(U)에 "세금계산서" 또는 "세금계산서 발급"이 있으면 해당 줄 끝 괄호 안에
`, 세금계산서 발급`을 덧붙인다. 근거가 없으면 임의로 붙이지 않는다.

---

## 4. 포맷팅 규칙

- **금액**: 천 단위 콤마 + `원`. 예: `1595000` → `1,595,000원`.
- **총 미수대금 금액은 별표로 강조**: `*1,595,000원*` (카카오톡/문자 강조 표기).
- **청구월**: `2026/05` → `26년 5월` (연도 두 자리).
- **인출일**: 결제일(M) `2026-06-04` → `6월 4일`. 월/일 앞의 0은 제거.
- 줄바꿈은 템플릿 그대로 유지한다.

---

## 5. 출력 템플릿

거래처 1곳당 아래 형식으로 1개 메시지를 만든다. `{ }`는 치환값,
`[선택]` 표시 줄은 해당 상황일 때만 넣는다.

```
[{회원명} 세무기장료 미수 안내]
안녕하세요 솔루션택스 팀입니다.
{오늘연도}년 {오늘월}월 현재 기준 {회원명} 미납금액이 있어 안내드립니다.

총 미수대금 : *{총미수대금}원*
{청구월줄1}
{청구월줄2}
...
[선택] {특이사항 안내 문장 — 직접입금/부분납부 등 거래처별 사정}

기장료 및 조정료에 대한 미수금이 존재하는 경우 계약 및 세무신고(부가세, 법인세 등)가 진행되지 않을수 있고 장기(3개월 이상)의 경우 계약해지 사유에 해당 합니다.

내용 확인하여 주시고 처리 가능한 일자와 방식(CMS / 이체 / 개인카드 결제 가능)을 말씀 주시길 바라겠습니다.
```

**청구월 줄 형식**:
```
{청구월} {상품표기} : {미납금액}원 ({인출일} {실패사유}{, 세금계산서 발급})
```
예: `26년 5월 월 기장료 : 165,000원 (6월 4일 인출 실패)`

- `{오늘연도}/{오늘월}`은 메시지 작성 시점 기준(미정이면 사용자에게 확인).
- `솔루션택스`는 발신 팀명 기본값이다. 다른 발신처명을 쓰라고 하면 교체한다.

---

## 6. 작성 예시

`주식회사 하늘드림홀딩스`(회원번호 `recpP1V6Pm`, 7건 합계 1,595,000원) 예
(회원명은 `(주)하늘드림홀딩스`로 약식 표기):

```
[(주)하늘드림홀딩스 세무기장료 미수 안내]
안녕하세요 솔루션택스 팀입니다.
2026년 6월 현재 기준 (주)하늘드림홀딩스 미납금액이 있어 안내드립니다.

총 미수대금 : *1,595,000원*
26년 1월 월 기장료 : 330,000원 (2월 3일 인출 실패)
26년 1월 월 기장료 : 165,000원 (2월 3일 인출 실패)
26년 2월 월 기장료 : 165,000원 (3월 6일 인출 실패)
26년 3월 결산조정료 : 440,000원 (4월 7일 인출 실패)
26년 3월 월 기장료 : 165,000원 (4월 2일 인출 실패)
26년 4월 월 기장료 : 165,000원 (5월 7일 인출 실패)
26년 5월 월 기장료 : 165,000원 (6월 4일 인출 실패)

기장료 및 조정료에 대한 미수금이 존재하는 경우 계약 및 세무신고(부가세, 법인세 등)가 진행되지 않을수 있고 장기(3개월 이상)의 경우 계약해지 사유에 해당 합니다.

내용 확인하여 주시고 처리 가능한 일자와 방식(CMS / 이체 / 개인카드 결제 가능)을 말씀 주시길 바라겠습니다.
```

---

## 7. 산출물 형태

- 거래처별 메시지를 **거래처마다 구분**해 출력한다(코드블록 또는 거래처명 머리글).
- 요청이 없으면 파일 저장 없이 메시지 텍스트만 제시한다.
- "표로", "CSV로", "거래처별 파일로" 등 형식 지정이 있으면 그에 맞춘다.
- 휴대전화번호(H)는 발송 대상 식별용이므로, 요청 시 거래처별로 함께 표기한다.
